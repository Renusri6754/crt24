int main() {
    int n,i=1,sum=0;
    scanf("%d",&n);
    while(i<=n)
    {
        if(i%2==0)
        {
        sum=sum+i;
        }
    printf("i=%d sum=%d\n",i,sum);
    i++;
    }

    return 0;
}
